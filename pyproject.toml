[project]
name = "cluster-group-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matin Fathi", email = "matinfathi1998@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
cluster-group-client = "cluster_group_client:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
